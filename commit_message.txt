feat: add global default note theme and UI/UX polish

New Features Added:
- Introduced a global default note theme preference (e.g., default, pastel-red, pastel-blue, pastel-green, pastel-yellow) in SystemSettings.js.
- Updated MongoDB user schema and authController to persist defaultNoteTheme.
- Refactored NoteModal to automatically inherit the user's defaultNoteTheme.

UI/UX Improvements & Polish:
- Integrated a visual theme palette directly into settings with active checkmarks and hover effects.
- Implemented sleek, transparent floating-pill custom WebKit scrollbars for the category chips row.
- Enforced flex-shrink: 0 constraints on category chips (tags) to fix severe squishing on desktop layouts.

Localization Updates:
- Added dynamic translations for new settings and Toast notifications.
- Injected accurate keys into en.json, my.json, and th.json to ensure full internationalization.

Bug Fixes:
- Fixed Dashboard sorting logic by correcting state mismatch where Header.js dispatched translation strings instead of functional state keys.
- Patched contrast and accessibility bugs in NoteCard and NoteModal for light pastel themes (enforced strict text/icon color overrides).
- Resolved broken validation logic in NoteModal during note updates.
